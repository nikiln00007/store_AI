{
  "name": "freelance-workspace-root",
  "private": true,
  "version": "1.0.0",
  "description": "Workspace root package forwarding commands to project-1",
  "scripts": {
    "dev": "npm run dev --prefix project-1",
    "build": "npm run build --prefix project-1",
    "start": "npm run start --prefix project-1"
  }
}
