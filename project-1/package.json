{
  "name": "dukaan-ai-root",
  "private": true,
  "version": "1.0.0",
  "description": "Store AI Root Workspace",
  "scripts": {
    "dev": "npx concurrently -n \"BACKEND,FRONTEND\" -c \"green,blue\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
